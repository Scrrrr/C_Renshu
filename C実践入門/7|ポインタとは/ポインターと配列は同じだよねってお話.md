ポインターと配列は密接な関係であるが、同じではない
``C
int array[2] = {1, 2};
int *ptr;

ptr = array; //これはできる
array = ptr; //これはできない！
``
int *ptrで宣言しているのはあくまでもint型のポインター変数。
ポインター変数はアドレスを格納するためのもの。
一方、arrayは配列なので、アドレスを入れるための変数ではないから


